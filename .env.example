# Stack Finder (OpenRouter)
OPENROUTER_API_KEY=

# Public site URL (no trailing slash)
# Local: http://localhost:3000
# Production: https://www.theprotocolau.com
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Convex (orders)
# Local: filled by `npx convex dev` (may be anonymous → http://127.0.0.1:3210)
# Production: cloud URL from `npx convex deploy` (https://….convex.cloud)
# Also set on Vercel Production: NEXT_PUBLIC_CONVEX_URL
CONVEX_DEPLOYMENT=
NEXT_PUBLIC_CONVEX_URL=
# Shared secret: Next.js payment routes → Convex order mutations
# Generate a long random string; set the SAME value in Convex AND on Vercel:
#   npx convex env set ORDERS_WEBHOOK_SECRET '<same-value>'
#   vercel env add ORDERS_WEBHOOK_SECRET production
ORDERS_WEBHOOK_SECRET=

# Freshnup MoonPay payment bridge (same PAYMENT_BRIDGE_SECRET on both Vercel projects)
PAYMENT_BRIDGE_SECRET=
FRESHNUP_PAYMENT_URL=https://www.freshnup.global/pay

# Stripe test mode — replace placeholders with Dashboard test keys.
STRIPE_SECRET_KEY=sk_test_replace_me
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_replace_me
STRIPE_WEBHOOK_SECRET=whsec_replace_me

# Direct self-custody crypto settlement
CRYPTO_ETH_ADDRESS=0x22ca069363df8cf72c1d900e001c218e1fb62025
# Optional: each unset address hides that currency at checkout.
CRYPTO_USDT_ADDRESS=
CRYPTO_BTC_ADDRESS=
# Optional Ethereum mainnet JSON-RPC URL enables automatic ETH/USDT verification.
# Without it, submitted Ethereum TXIDs enter pending manual review.
ETH_RPC_URL=
# Ethereum mainnet USDT contract; override only if intentionally changing network/token.
CRYPTO_USDT_CONTRACT=0xdac17f958d2ee523a2206206994597c13d831ec7

# Optional PayID/bank scaffold. Keep off until bank instructions are operational.
NEXT_PUBLIC_BANK_TRANSFER_ENABLED=false

# Convex backend (orders)

Orders for MoonPay checkout live here. Run `npx convex dev` from the `peptide/` directory to link a deployment and regenerate `_generated/`.

Do not run `npx convex deploy` unless shipping to production.
